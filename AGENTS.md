# SMART SAVE MASTER PROMPT

## STRICT DEVELOPMENT RULES (MANDATORY)

Before making ANY code change, follow these rules.

### Rule 1
Change ONLY the feature or page that the user explicitly requests.

### Rule 2
Never modify any other page, module, component, file, route, function, style, layout, database logic, calculation, login system, or business logic unless specifically asked.

### Rule 3
Never delete any existing feature.

### Rule 4
Never rename any feature.

### Rule 5
Never move any feature.

### Rule 6
Never overwrite an existing page with a new implementation.

### Rule 7
Never change colors, spacing, icons, sidebar, navbar, buttons, routing, permissions, calculations, reports, dashboards, settings, member pages, employee pages, receipts, collections, commissions, reminders, login, registration, profile, or any other module unless explicitly requested.

### Rule 8
If a requested change could affect another file or module, STOP and ask for confirmation before making any modification.

### Rule 9
Preserve 100% of the existing SMART SAVE project.
Do not remove or alter any existing functionality.

### Rule 10
Only ADD new functionality.
Never REPLACE existing functionality.

### Rule 11
If the user says "Change Button", then change ONLY that button.
If the user says "Change Login", then change ONLY the Login page.
If the user says "Change Members Page", then change ONLY the Members page.
Everything else must remain exactly the same.

### Rule 12
After completing the task, verify that no unrelated files were modified.
If any unrelated file was changed, restore it before finishing.

### FINAL RULE
SMART SAVE is now in Production.
Treat every existing screen, feature, calculation, report, setting, dashboard, route, and database logic as LOCKED.
Nothing may be changed unless the user explicitly requests that exact change.
Always preserve the entire application exactly as it is.

Checkpoint "SMART SAVE v1.0 Settings Restored" baseline established.
